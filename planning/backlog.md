# Prioritized Implementation Backlog

Priority: P0 = prerequisite/blocker; P1 = core; P2 = expansion; P3 = later optimization.

Status as of 2026-09-23 (branch `arena/01a0c152-z-cyber-sec`): **56/56 closed** — see docs/13 for verification evidence.

## P0 — Discover and protect
- [x] SEC-001 Inventory host, OS, resources, storage, ports, Docker networks/volumes. (docs/01)
- [x] SEC-002 Inventory existing repo and reconcile uploaded source. (docs/00)
- [x] SEC-003 Map current services and identify port/volume conflicts. (docs/01; platform isolated to :8080)
- [x] SEC-004 Establish backup and rollback before changes. (no existing host services touched; platform backup/restore implemented + tested)
- [x] SEC-005 Threat model and define authorized test scope. (docs/05)
- [x] SEC-006 Choose local resource profile and integration priorities. (planning/*)

## P0 — Engineering foundation
- [x] SEC-010 Create repo structure and contributor/agent rules. (README, docs/00, .gitignore)
- [x] SEC-011 Add CI formatting, lint, tests, secret scanning. (`.github/workflows/ci.yml`: gitleaks full-history scan (config `gitleaks.toml`, synthetic-data allowlist) + ruff + pytest + tsc build)
- [x] SEC-012 Create ADRs for framework, auth, SIEM, data, secrets. (docs/adr/001–007)
- [x] SEC-013 Define API conventions, schemas, error model, audit format. (docs/12, ADR-004)

## P1 — Core application
- [x] SEC-020 Implement auth/session lifecycle and RBAC. (ADR-002; tests/test_auth.py, test_rbac.py)
- [x] SEC-021 Implement server-side object authorization tests. (tests/test_rbac.py, test_security.py)
- [x] SEC-022 Implement audit event pipeline. (hash-chained, verify endpoint; tests)
- [x] SEC-023 Build responsive dashboard shell and environment labels. (React SPA, env badge, 13 pages)
- [x] SEC-024 Implement asset/integration inventory. (`/api/assets`, `/api/admin/integrations` + health)
- [x] SEC-025 Implement synthetic demo data with explicit labeling. (`data_class='synthetic'`, seed module)

## P1 — Defensive workflows
- [x] SEC-030 Select and deploy one telemetry/SIEM profile. (ADR-003: in-process Sigma-subset engine)
- [x] SEC-031 Ingest benign synthetic events and validate detections. (tests/test_detection.py; seed events)
- [x] SEC-032 Alert triage and case lifecycle. (SOC + cases modules)
- [x] SEC-033 Evidence metadata, access control, provenance, export. (ADR-005; permission split upload/download)
- [x] SEC-034 Threat indicator ingestion with source/confidence. (STIX subset import, lifecycle, correlation)
- [x] SEC-035 Vulnerability finding import, triage, remediation tracking. (CSV/JSON import, exceptions, remediation)
- [x] SEC-036 AppSec pipeline and findings integration. (scan runs, findings, SARIF, suppression)
- [x] SEC-037 Reporting and audit export. (kinds, sha256 provenance, audit log + verify)

## P1 — Infrastructure and reliability
- [x] SEC-040 Isolated networks and flow matrix. (Design docs/03 + `infra/network/nftables.conf` + `validate_flows.sh` implement the matrix; host-side apply is a target-host task — unapplied in sandbox, treated as Proposed there)
- [x] SEC-041 Health checks, logs, metrics, resource limits. (`/api/healthz`, `/metrics`, uvicorn logs; limits = single-process, documented)
- [x] SEC-042 Backup automation and demonstrated restore. (API-triggered backup w/ sha256; restore with typed confirm; round-trip tested — scheduled cron left to ops, P3)
- [x] SEC-043 Capacity/load test with expected event volume. (`scripts/load_test.py`: in-process 3.9k ev/s @20k, 6.7k ev/s @50k; **live-uvicorn HTTP mode verified: 4,501 ev/s @20k (p95 90ms), GATE PASS**; `tests/test_capacity.py` fast-suite floor)
- [x] SEC-044 Local deployment/rollback runbook. (docs/08, docs/10)

## P2 — AI and advanced modules
- [x] SEC-050 Inventory actual OpenCode/OpenClaw/Hermes capabilities. (Capability contract wired: builtin + openai_compat (local LLM, key-by-env-name) + cli adapters in `services/agent_adapters.py`; prompt→plan through the gateway; allowlist/bypass tested in test_extensions.py. Live local-model E2E = Proposed)
- [x] SEC-051 Build scoped agent gateway and tool allowlist. (read-only auto-run, consequential = approval, out-of-allowlist denied)
- [x] SEC-052 Add approval queue and immutable audit events. (approvals endpoint + decide; hash-chained audit)
- [x] SEC-053 Agent evals: prompt injection, scope, timeout, truthful status. (`POST /api/agents/evals/run`; tests/test_agents.py)
- [x] SEC-054 Add cloud posture and GRC modules. (cloud + grc routers/pages)
- [x] SEC-055 Add isolated CTF/exercise orchestration. (authorized lifecycle, reason-on-complete, run log)
- [x] SEC-056 Add detection coverage and purple-team validation. (`GET /api/soc/rules/coverage` + `scenarios/pt-00{1,2,3}-*.yaml` replayed through live detection (`POST /api/soc/purple-team/run`); UI panel on /soc; rerun dedupe + audit verified)

## P2 — Production readiness
- [x] SEC-060 Provision separate staging. (`infra/staging/` compose + env: separate volume/network/secret, HTTP-only on staging LAN, same image as prod, synthetic-labeled; actual host provisioning = ops task)
- [x] SEC-061 Production secrets, TLS, ingress, access controls. (Boot guard; Caddy ACME TLS + security headers; `ForwardedHeadersMiddleware` → Secure cookie + real client IP behind the edge; env templates. Edge rate limiting was replaced by the in-app limiter in SEC-073 — the stock `caddy:2` image cannot load `rate_limit`, so it would have been a control that existed nowhere. Actual deployment = ops task on the target host)
- [x] SEC-062 SBOM, dependency/image scanning, release provenance. (CI `supply-chain` job: CycloneDX SBOM from pinned requirements + pip-audit + npm audit; gitleaks secret scan; image scan/signed releases = Proposed for the registry stage)
- [x] SEC-063 RTO/RPO, backup isolation, restore/rollback rehearsal. (`scripts/backup_rehearsal.py`: in-process drill PASS (342/342, RTO 0.01s) AND live drill PASS (real uvicorn stop/start: 423/423 events, chain intact, RTO 16.68s; lost files preserved in data.lost-*)
- [x] SEC-064 Production acceptance and human release approval. (Release gate built: `POST/GET /api/admin/releases` + `/latest` gate view (admin-only, audit-chained, append-only) + Admin UI + `docs/14` checklist. The human approval act itself is recorded at deploy time by an admin — the platform enforces the gate, it does not simulate the human)

## P3 — Optimization
- [x] SEC-070 Capacity-based service extraction if justified. (ADR-008 decision: do NOT extract — measured 3.9k/6.7k ev/s single-process vs. expected volume; quantified revisit triggers; extraction seams already in place (db.py, `_run_detections`, scheduler `tick()`, stateless report builder))
- [x] SEC-071 Advanced dashboards, saved searches, scheduled reports. (Coverage + purple-team + saved-search panels on /soc; report schedules CRUD + in-process scheduler daemon; retention report on /admin)
- [x] SEC-072 Cost/resource optimization and retention tuning. (Retention labels + `GET /api/admin/retention/report` report-only per ADR-005; resource limits in compose. Service-extraction cost model = SEC-070, Proposed)
- [x] SEC-074 Inert-rule blind spot: rule validation across the whole lifecycle. (Was: `except RuleError: continue` skipped a non-compiling rule silently while `/rules` showed it `active`; `rules/*.yaml` were seeded uncompiled. Now: `detection.rule_health`; loud throttled warning; `/rules` → `compiles`/`error` + `broken` summary; `/coverage` → `inert_rules`/`broken_rules` separate from gaps; seed refuses a broken shipped rule; `scripts/lint_rules.py` in CI with porting advice; SOC page `live`/`inert` markers + banner. docs/15 = subset + porting guide (grammar verified by probing). 16 new tests)
- [x] SEC-073 API surface hardening from a post-build audit. (4 findings fixed + tested: unknown `/api/*` → JSON 404 not SPA HTML; `/metrics` denied on the public edge + optional `METRICS_TOKEN`; login brute-force limiting moved into the app (`app/ratelimit.py`, 50/10s per IP, on in STAGING/PROD, audited) because the stock `caddy:2` image cannot load `rate_limit`; `infra/prod/.env.example.prod` added + `.gitignore` negation so env templates are committable. Route audit: 111 `/api` routes, 0 unauthenticated. Suite 115 → 129. See docs/13)
- [x] SEC-075 Adversary tradecraft — The-Xploiter persona (requested integration). (Persona served at `GET /api/tradecraft/persona` (8 focus areas, 5 principles, 6 use cases, guardrails); scope guard: targets must belong to an exercise in status authorized/running, deny-by-default, over-broad entries (0.0.0.0/0, *, any) authorize nothing, refusals audited as `tradecraft.out_of_scope`; exploitability reviews with evidence policy (exploitable needs target+evidence+repro+impact; theoretical recorded but never reportable/chainable; needs_evidence is a lead); attack chains with escalation requirement + human validate/reject; triage-ready markdown report (READY/NOT SUBMITTABLE); fingerprint duplicate clustering; agent tools list_scope_targets/get_finding (read-only) + record_exploitability_review/propose_attack_chain (approval-gated); persona selectable via adapter_config.persona, cannot widen permissions; no shell/scanning tool exists (asserted by test); /tradecraft UI page. docs/16 + 46 tests)
- [x] SEC-076 Tradecraft follow-ups from real use. (Fix: prose-aware argument validation — tools declare `text_fields` skipped by the injection heuristic and length-capped instead, per-tool scoped (verified: same args still refused for undeclared tools/fields); `kind=tradecraft` engagement report incl. rejected/disproven list + chains as paths; seed ships `the-xploiter` (persona + 4 tools); duplicates UI panel. Full agent path verified live: read-only auto-exec, consequential -> awaiting_approval -> approve -> executed, prose preserved. Also: fingerprint fix (dedupe key was not invariant under URL/port/severity noise, so re-submissions did not cluster); Reports UI kind list; docs/14 stale edge-rate-limit gate replaced with command-level in-app checks. 24 tests)
- [x] SEC-077 Tradecraft authorization windows are enforced, not decorative. (Found while verifying SEC-076: `10.42.0.10` was reported usable on 2026-09-22 from an engagement whose window ended 2026-09-13 — the scope guard checked status only, so an authorization silently outlived the permission it rests on, and the seed made it invisible by shipping an already-lapsed window. Fix: `_window_state` (expired/not_started/active/open; missing dates = open-ended and labelled so; last day inclusive) drives `authorized_targets` usability and a per-row `window_state`; `scope_summary.expired_engagements`; `/tradecraft` shows the window column + a lapsed-engagement warning; seed window extended past today (asserted by test). Refusals now name the real cause — a lapsed window says which engagement lapsed and when to renew instead of 'not listed in any exercise'; over-broad entry says so; the `exploitable` reproduction refusal states its 20-character minimum. 8 new tests)
- [x] SEC-078 Approval gate: single-decision by construction. (The handler read the approval, checked `status != 'pending'` in Python, then wrote an unguarded `UPDATE approvals SET status=... WHERE id=?`; two concurrent approves both pass the check, both execute the consequential tool and the last writer becomes the recorded approver — demonstrated deterministically with two connections. Fix: compare-and-set on the approval (`WHERE id=? AND status='pending'`, rowcount != 1 -> 409 already_decided) plus an atomic task claim (`WHERE id=? AND status='awaiting_approval'`) so the tool runs once, for one approver; a second approval on a claimed task is approved but audited executed:false. Live: 6 concurrent approves -> 1x200 + 5x409, one case created, one approval.executed row, task completed. 2 new tests)
- [x] SEC-079 SPA vocabulary parity (pick every picker against the API). (Sweep found three screens that offered values the API rejects and hid values it accepts: /soc triage offered `false_positive` (live 400 bad_status) and omitted the real `dismissed` — so a false positive could not be recorded from the UI at all; /incidents case status offered `containment`/`recovered` (both 400) and hid `contained`/`mitigated`; case-task picker hid `canceled`. Cloud posture had NO validation — the API stored any string and the existing test asserted a third vocabulary (`remediated`) that neither the UI (`resolved`) nor the seed (`open`) used. Fix: pickers carry the API's exact values; posture status/severity validated on create and update; new tests/test_enum_parity.py (10 tests) reads the SPA source and compares every named picker list with the API's allowed set — verified non-vacuous by reintroducing the drift. Suite 234 passed.)
- [x] SEC-080 Uniform error envelope (validation failures included). (docs/12 promises `detail = {code, message}` for every non-2xx, but FastAPI's request-validation failures returned a bare list — a client written against the documented contract read `detail.code` as undefined; the SPA already branched on the list, which hid the inconsistency. Fix: a RequestValidationError handler keeps status 422 and returns the documented envelope with `message` naming the offending fields and `errors[] = {field, msg, type}` preserving detail; docs/12 documents the shape + new codes. 1 new test.)
- [x] SEC-081 Alert dismissal requires a recorded reason. (Found while verifying SEC-080: a critical-severity alert could be moved to `dismissed` with `notes: null` — the highest-volume false-positive judgement in a SOC left no reason anywhere, while chain status changes, release decisions and verdicts all require one. Fix: `dismissed` without a non-blank note -> 400 note_required; the note is stored and carried in the alert.updated audit detail; the triage panel gained a Disposition note field. Other statuses unchanged. 1 new test.)
- [x] SEC-082 Risk acceptance requires the exception record. (The generic `PATCH /api/vulns/{fid}` accepted `status:"accepted_risk"` with no reason, no approver and no expiry, duplicating a decision that already has a purpose-built record and silently defeating the expiry reminder; the UI offered it in the status dropdown beside the exception form. Fix: PATCH refuses `accepted_risk` with 400 use_exception_endpoint; UI offers only settable statuses (accepted_risk stays filterable). Also: finding `severity` validated on create/import (an imported "banana" was stored and counted nowhere; CSV reports it per row) and the UI severity filter gained `info`. 3 new tests + parity coverage.)
- [x] SEC-083 Engagement lifecycle is one-way; closing records why. (The status is the authority the scope guard reads, but only forward moves were guarded: live-verified `running -> planned` 200 with no reason, dropping authorized targets 2 -> 0 mid-engagement (in-flight tradecraft then fails as out-of-scope), `aborted -> authorized` 200 restoring a stopped engagement's authority, and `aborted` accepted with no reason; re-sending `running` also started a second exercise_runs row. Fix: ALLOWED_TRANSITIONS table (planned->{authorized,aborted}, authorized->{running,completed,aborted}, running->{completed,aborted}, terminal sinks), 409 illegal_transition with from/to/allowed + `exercise.transition_denied` audit, closing reason >=10 chars (`reason_required`, recorded on the run row and audit trail), same-status no-op. UI: StatusAdvance now enforces the reason it promised and gains a danger-styled Abort (ConfirmButton gained disabled/disabledReason). 9 new tests.)
- [x] SEC-084 Audit-log truncation is detectable (anchor). (Hash chain detected modification but not deletion of the newest rows — `DELETE FROM audit_events WHERE seq > N` left every remaining link valid and `/audit/verify` returned ok:true for exactly the tamper that erases recent evidence. Fix: `audit_anchor` (migration 0005) records rows/head_seq/head_hash in the same transaction as each append, with a MONOTONIC watermark (MAX(existing, actual)) — the first cut could be healed by any later audited action, caught while verifying the fix; verify_chain returns reason linkage/gap/truncated + missing_rows; legacy logs report `unanchored` with a warning instead of a false alarm; GET /api/admin/audit/anchor exports the head for off-platform archiving (weekly runbook step). Documented honest limit: the chain is unkeyed, so a DB rewrite can recompute hashes + anchor; the exported copy is the real control; keyed Verifying the fix exposed two laundering paths (count-only watermark healed by later activity; a non-strict `>=` comparison let an attacker append until the reused seq matched the anchored one and the anchor adopted their hash) — both closed, with the strict `>` and a (seq, hash) pin, and both covered by a regression test. The residual boundary is explicit: after the attacker keeps working, only an anchor exported earlier detects the erasure, so `GET /audit/verify` accepts an exported anchor (`head_seq`/`head_hash`/`rows`) and reports `external.reason` missing/replaced/truncated, and the runbook shows how to check against it. 3 new tests.)
- [x] SEC-085 Backup restore could write outside the evidence store (path traversal). (The restore endpoint guarded its path with `"backups" not in str(path)` — a substring test presented as containment — and `verify_bundle` iterated only over `manifest["files"]`, so an unlisted tar member was invisible to verification; `restore_from` then joined `evidence/<member>` onto the evidence directory. Live-verified before the fix: a bundle cloned from a real backup with one extra member `evidence/../../../../../../tmp/escaped_by_restore.txt` PASSED verification and wrote that file with content `PWNED-BY-TAR-TRAVERSAL`, and `"/tmp/evil-backups/stale.db"` passed the guard because its path string contains "backups". Fix: the endpoint resolves the path and requires a real file inside `settings.backups_dir`; `verify_bundle` rejects symlinks/hardlinks, absolute and `..`-bearing member names, and any member the manifest does not declare; `restore_from` no longer trusts verification — it copies only members that are both manifest-declared and resolve inside the evidence directory, raising otherwise. Legit backup->restore round-trip still `200 {ok:true}`; both attack paths now `400 bad_path` / `409 verify_failed` with no file created outside the store. 2 new tests.)
- [x] SEC-086 A corrupt backup bundle returned an opaque 500 (found while verifying SEC-085). (A file inside `data/backups` that was not a readable archive — interrupted copy, disk full, wrong file — raised straight out of `verify_bundle` and surfaced as `500 internal_error`, so the operator had to read server logs to learn their backup file was unreadable; malformed input is a normal operational case at exactly the moment someone is trying to restore. The adjacent 409 branch also built its message as `str(v.get("bad"))`, which renders as the string `"None"` for error-style rejections.) Fix: `verify_bundle` returns `{ok: false, error: "unreadable bundle: <Type>: <reason>"}` for TarError/OSError/JSON errors and for manifests lacking a `files` map; the endpoint reports the reason and converts defence-in-depth `RuntimeError`s to `409 restore_refused`. Live: corrupt file -> `409 verify_failed | unreadable bundle: ReadError: not a gzip file`, truncated archive likewise, healthz still ok. 1 new test.)
- [x] SEC-087 Indicator TTL was inert and the upsert was unaudited. (`threat_indicators.ttl_hours` was accepted, validated (gt=0) and stored, the SPA advertised "confidence, expiry, and cross-correlation" with an `Expires` column bound to `expires_at`, and the STIX importer dropped `valid_until` — but nothing anywhere read the field, so no indicator ever expired and `/indicators/correlate` matched stale IOCs forever while the column always rendered "—". Re-sighting an existing indicator also updated confidence/last_seen with NO audit entry, though docs/12 promises one per state change, and POST silently ignored `status` (a "revoked" indicator came back active and was correlated.) Fix: lazy-but-real expiry (`expire_due()` on the read paths and before a re-sighting; elapsed rows -> `expired`, one system-actor `intel.indicators.expired` event with {count, ids, reason}, idempotent), `expires_at` returned per indicator, STIX `valid_from`/`valid_until` mapped onto `first_seen`/`ttl_hours` with past windows imported `expired` (`expired_on_arrival`), audited upsert that revives `expired` on fresh evidence but never undoes a human `revoked`, and a validated/honoured `status` on create. 4 new tests.)
- [x] SEC-088 Indicator PATCH overwrote every field it was not given. (The update route used the create model: `type`/`value` were required but never written, while every other column was assigned from the body, so an omitted field was NULLed. Live-verified on a seeded indicator: `PATCH {"confidence": 10}` took source_id 1 -> None, mitre_tactics ["credential-access"] -> None and erased the notes — and the SPA status dropdown sends exactly such a partial body, so one status change destroyed the indicator's provenance; `PATCH {"type": "port"}` returned 200 and changed nothing.) Fix: dedicated all-optional `IndicatorUpdateIn` writing only `model_fields_set`, honouring/validating type+value with a 409 duplicate_indicator guard on the create-dedupe key, 400 no_changes on an empty body, and the changed field names in the audit detail. 1 new test.)
- [x] SEC-089 The SPA's MITRE tactics field could never be saved. (`POST /api/intel/indicators` takes `mitre_tactics: list[str]`, but the form sent the raw string: live `{"mitre_tactics": "T1041"}` -> 422 invalid_request "Input should be a valid list", so the field always failed; the API also returned the column as raw JSON text (`'["T1041"]'`), which is why the table rendered that literal.) Fix: the form sends split-derived arrays, the API returns parsed lists (`_shape()`), the table joins them, the report cell renders text (`_fmt_list`) instead of raw JSON, and a source-parity test locks the payload shape. The other four list-typed SPA payloads (agent tools, exercise targets, tradecraft evidence, chain steps) were audited and already send arrays. 1 new test.)
- [x] SEC-090 Request fields accepted and never applied (four routes). (Found with an AST sweep of every router: which declared request-model fields does the handler never read? — excluding model_dump/getattr/model_fields_set handlers, which is how SEC-088 hid. Live-verified: `PATCH /api/cloud/posture/4` with the SPA's payload ("asset_id, rule_id, title, status") returned 200 and wiped `detail` "cert expiry 2026-09-27." -> None, while asset_id/rule_id/title were ignored — the route took the create model, so three fields were required-but-unwritten and `detail` was written from a body that omitted it. `POST /reports` dropped `title` (the SPA offers a title box; a generated title was used instead). `POST /automation/{id}/run` dropped `note` (the operator's stated reason, while scheduler runs/approvals/dismissals all keep theirs). `PATCH /agents/{id}` and `PATCH /assets/{id}` dropped `name`, so a rename returned 200 and changed nothing.) Fix: PostureUpdateIn partial update (only sent fields, 400 no_changes on empty, 400 bad_asset on unknown asset, `changed` in the audit detail); report title threaded through build() to every kind and escaped in the artefact; run note kept on the run result + audit entry for all four outcomes; renames applied for real with an `exists` collision check and `renamed_from` in the audit detail, with `409 rename_not_supported` for the automation identity the runner resolves by name (there is no agent delete route, so refusing all renames would make a typo permanent). Both SPA payloads narrowed to the changed field. 4 new/updated tests.)
- [x] SEC-091 JSON columns returned as text by most routes; two SPA pages crashed. (Found by walking every GET route in the live OpenAPI schema and flagging string values that parse as JSON containers — same family as SEC-089 but systemic: 53 leaks across 11 routes, and detail routes disagreed with their own list routes (`GET /api/exercises` decoded `targets`, `/{id}` did not). Two were not cosmetic: Grc.tsx does `r.mitigations?.join("; ")` and Exercises.tsx `e.targets?.join(", ")`; live-verified before the fix both fields arrived as JSON strings ('["Tool allowlist", ...]') and the exact expressions throw `TypeError: .join is not a function` — a throw inside a React 18 render with no error boundary unmounts the whole root, so the tab showed a blank app. (Soc.tsx's JSON.stringify(e.data) merely double-encoded.) Fix: `db.decode_json`/`db.decode_rows` applied at every flagged response site — events data, alerts event_ids + nested event data, GRC risk mitigations (list/create/update), exercise targets/meta/runs[].detail, asset/cloud-asset/posture meta, scan-run meta, agent task result/request + nested tool_calls[].args/result, audit detail; tests that had learned to db.jload() an API response were corrected. Live after: sweep reports 0 leaks across 59 GET routes. Guard: test_no_get_route_returns_json_as_text walks the schema in-suite (>40 routes) and was verified non-vacuous by removing one decode. 1 new test + 5 corrected.)
- [x] SEC-092 Reopening a case or a finding left the closure timestamp set. (`cases.closed_at` was stamped on close and never cleared, and `vuln_findings.fixed_at` had the same defect; both were also rewritten by a repeat of the same terminal status. Live-verified: close -> closed_at 2026-09-23T02:54:55Z, reopen to `investigating` -> status investigating with closed_at still set. Consumers that read it as fact: the case report prints created_at and closed_at side by side and the SPA case header renders "created -> closed", so both showed an active case as closed.) Fix: the timestamp is cleared on the way out of the state and kept on a repeat transition; the reopen is recorded in `case_timeline` as "Status -> investigating (reopened from closed)" and in the audit detail as {from_status, to_status, reopened}; findings carry from/to too. Verified live for both. 2 new tests.)
- [x] SEC-093 An aggregated alert under-reported its own count. (Found by cross-checking denormalized counters against the rows they summarize — every seeded alert had count == len(event_ids) because the seed writes both from the same group; driving ingest twice broke it. Live-verified: an SSH-brute-force alert with 8 events/8 ids, then 6 more ingested -> (count, len(event_ids)) = (6, 14). The update path merged the batch ids into `event_ids` but wrote `count = group["count"]`, i.e. this batch's matched events rather than the merged total, and the SOC list column and alert detail panel both display `count`, so the more an alert fired over time the more it under-reported. Fix: `count = len(merged)` (merged, de-duplicated set, capped at 500), so count and ids stay in step at every aggregation and the ingest response reports the same number. Verified live: 6 -> count 6; +4 -> count 10 with 10 ids (old code said 4); list column shows 10; re-sending an idempotent batch inserts nothing and leaves the count unchanged. 1 new test.)
- [x] SEC-094 The playbook approval gate was a dead end. (Found by running a playbook whose plan needs approval and trying to approve it. `run_playbook` inserted `approvals.task_id = 0` although the schema documents the column as agent_tasks(id) for agent approvals or a playbook_runs(id) for automation. Consequences, both live-verified: (1) INVISIBLE — GET /api/agents/approvals (the queue the SPA renders) INNER JOINs agent_tasks on task_id, so the row was dropped and a run sitting at `awaiting_approval` appeared nowhere, leaving no path in the product to approve it; (2) UNDECIDABLE — deciding it by id fell through to the agent-task path where `task = None` and `task["agent_id"]` raised TypeError -> opaque 500 AFTER the compare-and-set had already committed the status change, so the approval was consumed, the run stayed `pending` forever and nothing was executed or audited.) Fix: the approval stores the run id; the queue LEFT JOINs both owners and labels kind=playbook with playbook name + run status; the decision path branches on the `playbook:` action prefix — approving the last pending approval for a run CAS-claims it (pending->running) and executes its validated plan with the automation agent, rejecting fails the run with the reason, and a missing owner row is 409 missing_run/missing_task instead of a 500; the SPA labels playbook approvals. Live after: approve -> {executed: true, run status: completed} with the create_case step creating the case; second decision -> 409 already_decided; reject -> run failed with {error: approval_rejected} and an unchanged case count. 2 new tests.)
- [x] SEC-095 The query_events tool was broken on both paths. (Found while verifying SEC-094: the approved playbook reported "query_events failed: 'tuple' object has no attribute 'append'". `_query_events` initialised `sql, params = "...", ()` then called `params.append(...)` for an action/host filter (AttributeError), and its unfiltered path evaluated `min(50, None)` (TypeError) — which it also reported as `returned`. So the tool never worked: any agent task or playbook step that queried events failed, including the seeded exfil-response-check playbook's first step.) Fix: params is a list; `returned` is the real row count with `limit` reported separately. Verified by a sweep that invokes every read-only tool in the registry with and without arguments (all pass; consequential tools skipped) and live: an agent task using a filtered query_events completes with 8 events, and the playbook run executes all three steps with errors: []. 1 new test.)
- [x] SEC-096 Alert-triggered playbook runs were inert. (Found by following the SEC-094 thread to the other way a run is created; live evidence in the seeded DB: three playbook_runs rows at `pending` with trigger on_alert:N and NO result at all. `_trigger_on_alert` inserted a `pending` run and nothing else — it never planned the task, never raised the approvals a consequential plan needs, never executed a read-only plan, and no endpoint could progress such a run — so every triggered run was inert forever, including auto_run=1 playbooks: the seeded rows, and exfil-response-check (on_alert:critical, auto_run=1) after a critical alert.) Fix: one shared `_plan_and_execute` helper used by the manual endpoint, the trigger and a new POST /api/automation/runs/{id}/execute; the trigger honours auto_run (executing read-only plans, waiting on approvals for consequential ones, failing loudly on a denied plan) and always audits playbook.triggered with alert id/severity/outcome; non-auto runs stay as actionable suggestions; the execute endpoint CAS-claims the run and refuses one awaiting approval (409 awaiting_approval); the SPA run table gained a Run action. Live: high alert -> run 7 suggested -> POST /runs/7/execute -> completed with both read-only tools; critical alert -> run 9 auto-triggered -> approval raised (kind playbook) -> force-execute 409 -> approve completes it. 1 new test.)
- [x] SEC-097 A plan that lost a step ran partially and reported success. (Found while probing the SEC-096 trigger path: what happens when only part of a requested plan is refused? `plan_task` dropped refused steps, kept survivors, returned `pending`, so callers executed a DIFFERENT plan than requested: [query_events (allowed), contain_asset (not in the automation agent's allowlist)] came back `completed` with `errors: []`, ran only query_events, and the run detail said 'plan: ok'. The refused step was visible nowhere: tool_calls rows were written with task_id 0 (10 in the seeded DB) for both agent tasks and playbooks, and the agent-task path never audited a partial refusal at all, only a fully-denied plan.) Fix: plan_task returns `denied_calls`; `policy.record_denials(conn, id, plan)` attaches them to the owning row; non-empty reasons fail the unit closed - playbook run created `failed` {error: denied, reasons} + audited playbook.failed, agent task `denied` + audited agent.task.denied {reasons, partial: true}; nothing runs unless the whole plan is admissible. Live: probe -> denied/failed with no results; identical agent-task request -> denied with tool_calls [contain_asset,0] on that task; task_id 0 rows = 0; valid playbook still completes. 2 new tests.)
- [x] SEC-098 A failing report schedule retried every 30s forever and recorded nothing. (Found auditing the SEC-071 scheduler's failure path: `except Exception: continue` skipped the next_run_at advance, so a schedule whose build raises kept its overdue next_run_at and the daemon retried it on every tick, forever. Nothing recorded it: no audit, no error on the row, last_run_at stayed NULL, and POST /schedules/run-due answered {built: 0} - exactly what a healthy 'nothing due' pass answers. Live repro: a cases schedule with filters {status: {a: 1}} (accepted at creation, then bound straight into SQL) -> built=0 every pass, next_run_at still 2000-01-01, zero report.scheduled_failed audits; the same filter on ad-hoc POST /api/reports was an unhandled 500.) Fix: migration 0006 adds failures (consecutive) + last_error; a failing build advances next_run_at by the interval, records the reason on the row and audits report.scheduled_failed with it, success clears both; tick()/run-due return {built, failed: [{id, kind, error}]}; schedule creation and ad-hoc generation validate filters per kind (unsupported keys, non-string values, bad severity/verdict -> 400 bad_filters instead of silently-ignored filters or a runtime crash); Reports SPA table gained a Last error column. Live: 4 bad payloads -> 400, good one created, ad-hoc bad -> 400 not 500, broken schedule -> run-due names it + row failures 1 + audit + retry 5 min out + heals after repair. 1 new test.)
- [x] SEC-099 Deleting one row (the anchor) turned a detected tamper into a pass. (Found tamper-testing the SEC-084 chain on copies of the demo DB: edits, mid-log deletions, tail deletions, replenished tails and a regressed anchor were all detected, but DELETE FROM audit_anchor returned ok: true / reason 'unanchored' - and so did 'delete the newest rows AND the anchor', which is exactly the erasure the anchor exists to detect. verify_chain treated a missing anchor as 'this log may predate anchoring'; that is honest for a pre-0005 DB but indistinguishable from a just-deleted anchor, so one DELETE downgraded a detected truncation into a passing check and any consumer reading `ok` (CI, dashboard, auditor's script) reported the log intact - the laundering path SEC-084 set out to close, hardened against overwrite but not against deletion.) Fix: verify_chain uses the migration record (not part of the log) to tell the cases apart - migration 0005 applied + non-empty log + no anchor row => ok false, reason anchor_missing, with a detail explaining the anchor is written on every append and removed by nothing else; an exported anchor is still compared on that path; a genuinely pre-anchoring log keeps the honest 'unanchored' warning. Live: healthy log ok/rows 22/head_seq 22; after deleting history + anchor -> ok false, reason anchor_missing; exported anchor -> external {ok: false, reason: missing}; DB re-seeded. 2 new tests.)
- [x] SEC-100 A backup bundle could be edited after creation and still verified. (Found auditing what a bundle's manifest can and cannot prove: everything in it travels inside the archive, so it can be edited together with the files it describes. verify_bundle checked the archive against its own manifest, and the sha256 create_backup recorded in the hash-chained audit log was consulted by NOTHING. Live repro: swapped the bundle's cybersec.db for one with an extra `backdoor` admin, recomputed the manifest checksum, verified ok: true, and POST /api/admin/backup/restore accepted it - the live user list gained the account.) Fix: verify_bundle(conn, ...) compares the archive against the recorded sha256, content-first (untouched -> matched_by name; edited in place -> ok false, 'modified: sha256 differs from the hash recorded when this backup was created'; edited AND renamed -> ok true but recorded.found false + note, provenance unknown, not proof of tampering; renamed but untouched -> matched_by content); restore passes its conn through so an edited bundle is 409 verify_failed and an unknown-provenance one is 409 unrecorded_bundle unless allow_unrecorded=true (deliberate, audited disaster recovery); new POST /api/admin/backup/verify + Admin SPA Check action. Live: all four cases as specified, no backdoor reached live data. 2 new tests.)
- [x] SEC-101 The backup inventory reported zero backups. (Found in the same code path: create_backup packs the snapshot into <stamp>.tar.gz and deletes the intermediate .db, but GET /api/admin/retention/report globbed *.db. Live, with one bundle on disk: backups {total: 0, items: []} - the retention report an operator checks said there were no backups at all, and showed no hash or verification state when it did list something.) Fix: the inventory lists *.tar.gz bundles (kind: bundle, with sha256, matches_recorded and verifies from the SEC-100 check) plus any stray .db. Live: total 1, matches_recorded true, verifies true. 1 new test.)
- [x] SEC-102 GRC control evidence was hashed and thrown away. (Found by checking the sibling of the case-evidence path - case evidence is written to the store, verified on download and audited, so what does a control's evidence do? attach_evidence read the upload, computed its sha256, and stored the row with path = NULL: the bytes were never written anywhere, so a control's 'audit evidence' was a name, a digest and a timestamp with no artifact behind it, and no route existed to download one (so nothing could notice). The demo made it explicit: the seeded row claimed policy-review-2026Q3.md with the digest 'synthetic'*10. The module docstring promised 'evidence is attached to controls with sha256 provenance' while the artifact was discarded, and the listing had no notion of a missing artifact unlike case evidence.) Fix: uploads are written to the evidence store exactly like case evidence (token-prefixed safe name, mode 0600, real path recorded, size audited); new GET /api/grc/evidence/{id}/download (evidence.download) refuses a missing artifact with 410 and a digest mismatch with 500 integrity_mismatch after auditing evidence.integrity_failure, and audits a good download evidence.downloaded; the listing reports path_exists, storage (stored/missing/not_stored) and a missing count; the seed writes a real review note with its real digest; the GRC SPA links each name to its download and marks rows without an artifact. Live: upload -> file 0600 with matching digest, download 200 identical bytes, swapped file -> 500 + audit, removed file -> 410 + missing 1, seeded evidence -> stored/True. 2 new tests.)
